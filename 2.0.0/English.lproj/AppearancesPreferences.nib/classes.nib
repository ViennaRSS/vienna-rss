{
    IBClasses = (
        {
            ACTIONS = {
                changeMinimumFontSize = id; 
                selectArticleFont = id; 
                selectFolderFont = id; 
                selectMinimumFontSize = id; 
            }; 
            CLASS = AppearancesPreferences; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                articleFontSample = NSTextField; 
                articleFontSelectButton = NSButton; 
                enableMinimumFontSize = NSButton; 
                folderFontSample = NSTextField; 
                folderFontSelectButton = NSButton; 
                minimumFontSizes = NSComboBox; 
            }; 
            SUPERCLASS = NSWindowController; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }
    ); 
    IBVersion = 1; 
}