# GitHub Actions runners: https://github.com/actions/runner-images

name: Build and test

on:
  pull_request:
    paths:
      - Vienna*/**
      - External/**
      - Scripts/**
      - Makefile
      - .github/workflows/*

jobs:
  test:
    strategy:
      matrix:
        include:
          - os: macos-15
            xcode: Xcode 16.4
            developer-dir: /Applications/Xcode_16.4.app/Contents/Developer
          - os: macos-26
            xcode: Xcode 26.6
            developer-dir: /Applications/Xcode_26.6.app/Contents/Developer
    name: Test with ${{ matrix.xcode }}
    runs-on: ${{ matrix.os }}
    env:
      DEVELOPER_DIR: ${{ matrix.developer-dir }}
    timeout-minutes: 15

    steps:
    - name: Set up Git repository
      uses: actions/checkout@v7
      with:
        fetch-depth: 0
    - name: Build Vienna
      run: |
        set -o pipefail
        xcodebuild -version
        xcodebuild build-for-testing -project Vienna.xcodeproj -scheme Vienna -configuration Development | xcbeautify --renderer github-actions
    - name: Run unit tests
      run: |
        set -o pipefail
        xcodebuild -version
        xcodebuild test -project Vienna.xcodeproj -scheme Vienna -configuration Development | xcbeautify --renderer github-actions
